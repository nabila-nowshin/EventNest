import React from 'react';

const Loader = () => {
    return (
        <div>
            <span className="loading loading-bars loading-md"></span>
        </div>
    );
};

export default Loader;